galaxy_info:
  author : Your name
  description: Your role description
  company: Your company (optional)
   
  # If the issue tracler for your role is not on github, uncomment the
  # next line and provide a value
  # issue_tracker_ur;: http://example.com/issue/tracker
  #
  # Chooes a valid license ID from https;//spdx.org - some suggested license:
  # -BSD-3-Clause(default)
  # -MIT
  # -GPL-2.0-or-later
  # -GPL-3.0-only
  # -Apache-2.0
  # -cc-by-4.0
  
  license: license (GPL-2.0-or-later, MIT , etc)

  min_ansible_version: 2.9

  # If this a Container Enabled role, provide the minimum Ansible Container version.
  # min_ansible_container_version:
  #
  #
  # Provide a list of supported platforms, and for each platform a list of version.
  # If you don't wish to enumarate all version for a particular platform, use 'all'.
galaxy_tags: []

dependencies: []


